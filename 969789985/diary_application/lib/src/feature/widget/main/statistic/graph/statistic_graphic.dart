import 'package:flutter/material.dart';

class StatisticGraphic extends StatelessWidget {
  const StatisticGraphic({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}